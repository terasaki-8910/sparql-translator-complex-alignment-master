PREFIX rdf: <http://www.w3.org/1999/02/22-rdf-syntax-ns#>
PREFIX cmt: <http://cmt#>

SELECT ?conference ?name ?date ?reviewsPerPaper
WHERE {
  ?conference rdf:type cmt:Conference .
  ?conference cmt:name ?name ;
              cmt:date ?date ;
              cmt:reviewsPerPaper ?reviewsPerPaper .
}